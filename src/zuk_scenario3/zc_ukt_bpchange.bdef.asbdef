projection implementation in class ZBP_C_UKT_BPCHANGE unique;
strict ( 2 );
use draft;
define behavior for ZC_UKT_BPCHANGE alias ZcUktBpchange
use etag

{
  use create;
  use update;
  use delete;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;
  use action Prepare;
}